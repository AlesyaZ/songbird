import React from 'react';
import ReactDOM from 'react-dom';
import DataBirds from './components/data';
import App from './components/app';

import './main.scss';

ReactDOM.render(
    <React.StrictMode>
        <App />
    </React.StrictMode>,
    document.getElementById('root')
);
